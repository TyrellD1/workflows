# Placeholders

When creating a workflow in this repo:

- `SoRs.yaml` is a template (`REPLACE_ME_*`). No machine-specific ids.
- `Taste/` is a stub that says it is filled at CONFIG time. No local product taste, DB taste, or UX taste unless the user is explicitly defining canonical authoring taste for a meta workflow.
