# Finalize

Checklist. All must be true:

- [ ] SKILL.md is pointers only (title, description, steps, conditions)
- [ ] Each process is its own file under `Processes/`
- [ ] Every HiTL template was shown and approved
- [ ] `SoRs.yaml` and `Taste/` are placeholders (unless this is canonical authoring taste on a meta workflow)
- [ ] CONFIG.md exists
- [ ] README is human-facing and has mermaid
- [ ] Logs: start, each HiTL, done

Then log `--event done`. Commit per AGENTS.md.
