# Finalize

Checklist. All must be true:

- [ ] Mermaid was signed off in chat before SKILL.md was written
- [ ] SKILL.md is pointers only and matches the signed mermaid
- [ ] Each process is its own file under `Processes/`
- [ ] Process nodes on the mermaid are labeled `Process <name>`
- [ ] HiTL nodes on the mermaid are labeled `HiTL <template>`
- [ ] Each escalation HiTL file has a2h + Chat (and Doc only if needed)
- [ ] `SoRs.yaml` and `Taste/` are placeholders (unless this is canonical authoring taste on a meta workflow)
- [ ] CONFIG.md lists every SoR and Taste pointer and where to edit
- [ ] README is human-facing and uses the signed mermaid
- [ ] Logs: start, each HiTL, done

Then log `--event done`. Commit per AGENTS.md.
